class Registro(object):
    fecha_peso=[]

    def setAgregarFechaPeso(self, registro):
        self.fecha_peso.append(registro)

        def setFechaAltura(fecha, altura):
            for i in self.setAgregarFechaPeso:
                if fecha in self.fecha_peso and altura in self.fecha_peso:
                    return self.altura[i], self.fecha[i]

        def setPromedioPeso(self):
            for item in self.setAgregarFechaPeso in year:
                sum(self.peso[item])
                total = sum

            return total / len(self.fecha_peso)

        def setPromedioAltura(self):
            for item in self.setAgregarFechaPeso in year:
                return sum(self.altura) / len(self.fecha_peso)

